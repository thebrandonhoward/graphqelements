type Book {
    id: ID
    name: String
    pageCount: Int
    author: Author
}

type Author {
    id: ID
    firstName: String
    lastName: String
    reviews(filter: Int): [Review]
}

type Review {
    id: ID
    message: String
    rating: Int
}
